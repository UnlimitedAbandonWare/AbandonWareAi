policies:
  recency:
    retrieval.policy: recency_first
    retrieval.k_allocation.web_top_k: +8
    naver.search.timeout-ms: +500
    whitelist_override: conditional
  max_recall:
    expansion.query_burst: "15-24"
    expansion.extreme_z: "18-30"
    retrieval.k_allocation.vector_top_k: +8
    whitelist_override: conditional
  speed_first:
    budget.time_ms: -6000
    rerank.pass2: disabled
    retrieval.k_allocation.web_top_k: -8
    naver.search.timeout-ms: -800
  wide_web:
    whitelist_override: on
    safety.citation_gate_min_sources: 4

# NineTile alias corrector toggle
alias:
  corrector:
    enabled: true

probe:
  search:
    enabled: false
  admin-token: ''


retrieval:
  vector:
    enabled: true