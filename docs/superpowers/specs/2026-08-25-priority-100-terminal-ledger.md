# Priority-100 Terminal Ledger

- Date: 2026-08-25
- Authoritative inventory: `docs/superpowers/specs/2026-08-24-priority-100-risk-burn-down-design.md`
- Branch / frozen HEAD: `codex/owned-runtime-browser-restart` / `0796a3c5b29bbb08c3314bd40649d856d4a7bce6`
- Rule: each rank receives exactly one of `FIXED`, `NO_PATCH_NEEDED`, `EVIDENCE_NEEDED`, or `HOLD`; a later fresh gate may replace a provisional `HOLD` with a stronger terminal result.
- Current repository gate: after ranks 22/23 verification created a new 0-byte `.git/index.lock`, the user explicitly authorized recovery. Its canonical path, exclusive access, Git/Git-LFS and IDE process counts, source-edit lease state, and PatchDrop queue were revalidated; it was moved recoverably into `.git/codex-stale-locks/`, and the canonical lock path remains absent. Evidence is in `2026-08-25-priority-100-rank22-26-execution-journal.md`.
- Terminal coverage: `100/100`; disposition totals are `FIXED=29`, `NO_PATCH_NEEDED=31`, `HOLD=22`, `EVIDENCE_NEEDED=18`.

## Current dispositions

| Rank | Disposition | Fresh evidence summary |
|---:|---|---|
| 1 | `FIXED` | Bounded unauthenticated n8n body intake; focused, affected, full, review, and JAR proof in Cohort B1 progress. |
| 2 | `FIXED` | Vector queue retained capacity, failure accounting, recovery, caller contracts, independent approval, full/JAR proof in C1 journal. |
| 3 | `FIXED` | Pending-memory promotion remains retryable until durable flush; Cohort A focused/full proof. |
| 4 | `FIXED` | Indexing reinforcement/cursor advancement gated on durable vector outcome; Cohort A focused/full proof. |
| 5 | `HOLD` | `holdScope=image-job-atomic-claim`; `firstBlockingRule=source-owner-unproven`; `blockingEvidence=ImageJobRepository.java untracked at frozen intake`; `independentWorkCompleted=vector/pending/indexing fixes and atomic-claim design`; `repositoryWideHold=false`. |
| 6 | `FIXED` | Internal Brave session header removed from external request; outbound denial test and full proof in Cohort B1. |
| 7 | `FIXED` | Owned local-LLM child termination and lifecycle proof in C2 journal. |
| 8 | `FIXED` | Hybrid search await paths preserve caller interruption; C3 focused/broad proof. |
| 9 | `FIXED` | Self-Ask timeout terminates owned provider work and recovers executor capacity; C4 proof. |
| 10 | `FIXED` | Fingerprint aggregate state is bounded and evicted; C5 proof. |
| 11 | `FIXED` | Admin debug SSE uses finite timeout, bounded no-queue runtime, exact cancellation, recovery, and shutdown; C6 proof. |
| 12 | `FIXED` | Nonterminal chat runs expire by bounded idle timeout with exact identity fencing, one cleanup reservation, replacement safety, and owned shutdown; C7 affected 79 and full 6,473-test proof. |
| 13 | `FIXED` | Attachment metadata TTL/session cleanup and scheduler governance proof in Cohort B2. |
| 14 | `FIXED` | Active `ChatWorkflow` now rejects a bare generic output label derived only by stripping Korean `만` in the before-marker direct-literal path, while alphabetic literal, explicitly named value, and punctuated machine-token controls remain direct. Focused RED `7/4` became GREEN `7/0`; affected direct-literal tests, full `6,480` tests, source-set hygiene, and LangChain4j `1.0.1` purity passed. Browser/provider/wire execution remains `not_observed`; this disposition is the active routing-contract fix, not an external-success claim. |
| 15 | `FIXED` | The active `@Primary` `com.example.lms.vector.FederatedEmbeddingStore` now sends the UUID returned by `add(Embedding)` directly through the explicit-ID fan-out seam, so it cannot generate a second storage ID. The live RED also exposed an earlier LangChain4j `text cannot be null or blank` failure from the old empty-segment construction; the shared repair removes that invalid segment. A real `InMemoryEmbeddingStore` regression proves the returned ID equals the upstream match ID. Focused `2/2`, affected `11/11`, full `6,482`, source-set hygiene, and LangChain4j `1.0.1` purity are GREEN; see `2026-08-25-priority-100-rank15-16-execution-journal.md`. |
| 16 | `FIXED` | The same single-entry write seam now represents an absent segment with the null-tolerant `Collections.singletonList`, rather than `List.of(null)`. The explicit-ID/no-segment API completes, preserves the supplied ID upstream, and retains a null embedded value in the real in-memory store regression. This rank links to the same one-time production patch and verification evidence as rank 15. |
| 17 | `FIXED` | Active root compilation now gives `infra.cache.SingleFlightCache` an explicit loader election: each caller creates a candidate future, only the `putIfAbsent` winner runs the loader, followers await the existing future, and cleanup uses `remove(key,candidate)` identity fencing. The deterministic latch RED observed two loader calls and GREEN observed one shared `owner-value`; the existing untracked tracing test remained untouched. Focused `1/1`, affected `2/2`, full `6,483`, source-set hygiene, and LangChain4j `1.0.1` purity are GREEN. Internal production caller count remains `0`, so runtime invocation is `not_observed`; this disposition fixes the compiled public API contract only. See `2026-08-25-priority-100-rank17-execution-journal.md`. |
| 18 | `NO_PATCH_NEEDED` | Reported app-clean ONNX pass-through is excluded from Java compilation and every JAR; no bean/caller exists. Canonical root ONNX path is default-disabled and fail-soft. |
| 19 | `FIXED` | Active root `QueryTransformer` now owns the exact in-flight `FutureTask`, dispatches it through `execute(...)` so CancelShield cannot replace its cancellation handle, uses interrupting cancellation for caller interruption and force-kill, and restores both request-thread and worker interrupt signals. The real blocked-worker RED failed `3/3` before the patch and passed `3/3` after it; affected QueryTransformer/CancelShield suites passed `63/63`, the full suite passed `6,486`, and source-set/LangChain4j gates are GREEN. External provider/wire execution remains `not_observed`; see `2026-08-25-priority-100-rank19-93-execution-journal.md`. |
| 20 | `FIXED` | Active default JSONL `FileDegradedStorage.putPending` now persists request-side records through a separately locked on-disk ingress and only attempts maintenance with `tryLock`, so a blocked full-file retention sweep cannot queue another put. Maintenance atomically rotates and merges ingress before consumer/diagnostic reads, direct JSONL append remains unchanged when on-write enforcement is disabled, and crash recovery retains the existing duplicate-over-loss preference. The real blocked-read latch RED failed on the second put's 750 ms timeout and became `1/1` GREEN with both literal events returned by `claim(10)`; affected tests passed `28/28`, the full suite passed `6,487`, and source-set/LangChain4j gates are GREEN. Cross-process safety and provider/wire execution remain `not_observed`; see `2026-08-25-priority-100-rank20-execution-journal.md`. |
| 21 | `FIXED` | Active request-side `DebugEventStore` now sends only the already serialized/redacted NDJSON line to one lazy daemon writer backed by a bounded FIFO; `createDirectories` and `writeString(APPEND)` no longer run under the request thread's global store monitor. Capacity defaults to 256 and is capped at 8,192; saturation retains older work and rejects the newest line without blocking, with fixed count-only diagnostics. The latch RED failed on the six absent writer seams and became `1/1` GREEN; affected tests passed `50/50`, full tests passed `6,488`, and source-set/LangChain4j gates are GREEN. Console JSON logging remains caller-side, and live disk/provider/wire execution remains `not_observed`; see `2026-08-25-priority-100-rank21-execution-journal.md`. |
| 22 | `FIXED` | Active `AutoEvolveDebugStore.cleanupOldRotations` now owns the rotation-directory `Files.list` stream with try-with-resources. The focused ownership RED failed before the patch and passed afterward; combined ranks 22-26 affected tests are `66/66`, and the fresh full suite is `6,494/6,494` with zero failures/errors. |
| 23 | `FIXED` | The same active owner now closes the startup `loadRecentNdjson` directory listing with try-with-resources. Startup persistence/load coverage is GREEN, source-set/LangChain4j gates pass, and the shared final verification is recorded in the ranks 22-26 journal. |
| 24 | `FIXED` | `InMemoryJobService` retains only the newest 256 terminal status records under one short owner-local lock and conditionally removes the exact oldest terminal mapping, without evicting unrelated PENDING/RUNNING state. The 257-completion RED observed all 257 retained before the patch and exactly 256 after it, with oldest `NOT_FOUND` and newest `SUCCEEDED`. |
| 25 | `FIXED` | The same service now owns a bounded context-aware executor (`2` core, `4` max, queue `64`, `AbortPolicy`), marks rejected jobs `FAILED`, and has idempotent Spring `@PreDestroy` interruption/shutdown. Deterministic saturation and context-close REDs became GREEN; job/API affected tests are `21/21`. |
| 26 | `FIXED` | `ImageJobService` now snapshots recent durations under one private short lock and guards scheduler add+trim under the same lock; repository/provider work and average calculation remain outside it. A latch-controlled real `LinkedList` traversal reproduced `ConcurrentModificationException` before the patch and completed with a stable ETA after it; image affected tests are `35/35`. |
| 27 | `HOLD` | `holdScope=retrieval-minimum-allocation`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock plus untracked active owner`; `independentWorkCompleted=budget<source-count zero-allocation math, focused RED and effective-budget policy identified`; `repositoryWideHold=false`. |
| 28 | `HOLD` | `holdScope=model-runtime-snapshot-retention`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock plus dirty tracker ownership`; `independentWorkCompleted=unbounded provider-model snapshot cardinality, neighboring bounded structures, capacity/eviction RED and bounded-cache seam identified`; `repositoryWideHold=false`. |
| 29 | `NO_PATCH_NEEDED` | The scanned `com.example.lms.guard.AnswerSanitizer` implementations have no injection point or caller on the active answer path. `ChatWorkflow` finalization is owned by `FinalAnswerPostProcessor`/`OutputSanitizer`; wiring dormant guards would create a second policy owner. |
| 30 | `NO_PATCH_NEEDED` | `UniversalGuardSanitizer` does log a detected violation and return unchanged content, but no production answer-path consumer invokes it. Enforcing a future domain-confusion policy requires a separate product decision and structured result contract, not a speculative live-path patch. |
| 31 | `NO_PATCH_NEEDED` | `UniversalDomainSanitizer` has unfinished rules and pass-through behavior, but it and its deprecated wrapper have no active answer-path caller. A future enforcement change first requires one approved policy owner and explicit forbidden-rule evidence. |
| 32 | `HOLD` | `holdScope=fallback-domain-term-alternatives`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock`; `independentWorkCompleted=empty implementation and exact default-disabled legacy caller traced; positive/negative/neutral RED cases and bounded allowlist design identified, but an approved evidence-backed domain alternative catalog is also required`; `repositoryWideHold=false`. |
| 33 | `HOLD` | `holdScope=settings-entrypoint-ownership`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock plus dirty settings.gradle/settings.gradle.kts`; `independentWorkCompleted=fresh Gradle --info proves settings.gradle shadows materially different settings.gradle.kts; single-entrypoint acceptance probe identified`; `repositoryWideHold=false`. |
| 34 | `HOLD` | `holdScope=dependency-lock-generation-and-tracking`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock`; `independentWorkCompleted=all-project locking is enabled but current tracked and unignored lockfile counts are both zero; locked/offline verification identified`; `repositoryWideHold=false`. |
| 35 | `HOLD` | `holdScope=https-host-split-jar-resolution`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock plus untracked launcher script`; `independentWorkCompleted=launcher fallback traced to build/libs only while active split output is build/<host>/libs; fail-closed resolver RED identified`; `repositoryWideHold=false`. |
| 36 | `HOLD` | `holdScope=https-artifact-identity-selection`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock`; `independentWorkCompleted=stale default JAR and current Desktop JAR hashes, sizes and timestamps confirmed; rank-35 selection repair and identity preflight cover the causal risk`; `repositoryWideHold=false`. |
| 37 | `EVIDENCE_NEEDED` | Exact prior count 6,492 is stale: current readable top-level enumeration is 3,234 build directories, 3,031 desktop-prefixed. Cleanup is destructive and needs an owner-approved retention policy plus a bounded candidate manifest; no source patch is justified. |
| 38 | `NO_PATCH_NEEDED` | `app/src/main/java` still contains exactly 390 Java files/16,503 lines, but fresh Gradle source-set proof excludes it and `checkSourceSetHygiene` reports it only as inactive-present. Archival/deletion is a separate ownership decision. |
| 39 | `NO_PATCH_NEEDED` | The stale count 173 is now 166 inactive-vs-active duplicate FQCNs. `app/src/main/java` is absent from every active source set, so these classes cannot collide at runtime; retain the source-set quarantine assertion. |
| 40 | `NO_PATCH_NEEDED` | Exactly 12 root/app-clean active duplicate FQCNs remain in source, but all 12 are excluded from app compilation/packaging (10 generated stereotype exclusions plus two hard-list-only cases), leaving zero observed classpath collisions. Policy risk is tracked separately in ranks 41-43. |
| 41 | `HOLD` | `holdScope=app-duplicate-policy-fail-closed`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock plus modified app/build.gradle.kts and dirty duplicate population`; `independentWorkCompleted=default stereotype/none fail-open path and isolated non-stereotype fixture RED/all:fail repair identified`; `repositoryWideHold=false`. |
| 42 | `HOLD` | `holdScope=app-duplicate-report-contract`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock plus modified app build and 14 dirty app-clean paths`; `independentWorkCompleted=current 12 duplicates, 10 automatic exclusions, zero kept classes and default-silent reporting recalculated`; `repositoryWideHold=false`. |
| 43 | `HOLD` | `holdScope=app-hardcoded-duplicate-exclusion-drift`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock plus modified app/build.gradle.kts`; `independentWorkCompleted=17 static patterns applied at compile and jar sites, 12 current duplicates covered, stale ownership wording and post-all:fail cleanup contract identified`; `repositoryWideHold=false`. |
| 44 | `HOLD` | `holdScope=root-app-resource-ownership`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock plus 47 dirty resource paths`; `independentWorkCompleted=113 root/69 app resources recalculated with 28 collisions, 22 differing and six identical; ownership contract and first non-application cohort identified`; `repositoryWideHold=false`. |
| 45 | `NO_PATCH_NEEDED` | Both differing `logback-spring.xml` files are packaged, but the current Boot artifact resolves the root `BOOT-INF/classes` copy before the nested app JAR. The app copy is shadowed, not independently applied. |
| 46 | `NO_PATCH_NEEDED` | Both model manifests are packaged, but `ModelManifestConfig` performs one classloader lookup and deterministically selects the root resource. The app-only routing extension is shadowed and has no consumer in the active model registry. |
| 47 | `NO_PATCH_NEEDED` | Tool manifests differ, but `ToolManifestCatalog` returns the first existing candidate; the root top-level manifest is present and wins before either app fallback candidate. |
| 48 | `NO_PATCH_NEEDED` | `spring.factories` and auto-configuration imports are intentionally additive metadata, not single-resource competitors. Current entries register distinct owners (with framework de-duplication for the repeated configuration class), and no duplicate-bean/runtime failure was established. |
| 49 | `NO_PATCH_NEEDED` | App exclusions deliberately remove `plans/**` and every `application*` form, and current embedded app JAR contains no app application config. Broadly excluding additive metadata or fallback manifests would be unsafe without a separate owner decision. |
| 50 | `HOLD` | `holdScope=root-config-property-source-precedence`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock plus dirty YAML and ignored-but-packaged application.properties`; `independentWorkCompleted=current Boot JAR proves both root files package and properties override overlapping YAML keys; canonical-owner context RED identified`; `repositoryWideHold=false`. |
| 51 | `HOLD` | `holdScope=local-model-base-url-config-ownership`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock plus ignored packaged properties and dirty tracked YAML/profile owners`; `independentWorkCompleted=environment-aware and literal local-model definitions traced, including clean-checkout reproducibility risk; non-sensitive effective-property RED identified`; `repositoryWideHold=false`. |
| 52 | `HOLD` | `holdScope=kakao-redirect-config-ownership`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock plus dirty YAML and ignored packaged properties`; `independentWorkCompleted=two redirect defaults, properties precedence, actual /kakao/oauth/callback controller path and focused environment RED identified`; `repositoryWideHold=false`. |
| 53 | `HOLD` | `holdScope=ocr-enabled-config-ownership`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock plus dirty YAML and ignored packaged properties`; `independentWorkCompleted=effective hard-enabled override in current packaged artifact and base/profile context RED identified`; `repositoryWideHold=false`. |
| 54 | `HOLD` | `holdScope=ocr-confidence-config-ownership`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock plus dirty YAML and ignored packaged properties`; `independentWorkCompleted=materially different thresholds and properties precedence in current artifact confirmed; single-owner bind RED identified`; `repositoryWideHold=false`. |
| 55 | `NO_PATCH_NEEDED` | Active Reactor Netty wiretap defaults already false; Cohort B2 regression proof. |
| 56 | `FIXED` | Ultra-profile stacktrace exposure made safe by default; Cohort B2 proof. |
| 57 | `FIXED` | Ultra-profile verbose sensitive logging defaults made safe; Cohort B2 proof. |
| 58 | `FIXED` | Learning-profile destructive DDL default changed to validate; Cohort B2 proof. |
| 59 | `NO_PATCH_NEEDED` | Five `OutboxSendTool` meanings exist, including no-op success variants, but none is reachable from the active component scan or an exact production caller. The only durable component sits outside the scanned packages; consolidation needs a future owner/caller decision. |
| 60 | `EVIDENCE_NEEDED` | The exact Gradle-9 warning count is not current and the available logs mix Java compiler deprecations with Gradle compatibility claims. Refresh with an owner-coordinated isolated `--warning-mode all` build before changing or suppressing anything. |
| 61 | `HOLD` | `holdScope=source-health-large-file-metric`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock plus dirty source_health_scorecard.py`; `independentWorkCompleted=fresh active-root recount finds 16 files over 2,000 lines while the report says zero; stale-zero RED and recompute/freshness design identified`; `repositoryWideHold=false`. |
| 62 | `HOLD` | `holdScope=source-health-input-provenance`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock plus dirty scorecard script`; `independentWorkCompleted=fixed decision-input paths, two-argument CLI and implicit canonical overwrite traced; explicit path/hash/freshness RED identified`; `repositoryWideHold=false`. |
| 63 | `EVIDENCE_NEEDED` | Completion-audit artifact is now about 808,096 seconds old and exceeds the 24-hour gate. Its old green payload cannot prove current completion; generate exactly one fresh local completion audit and retain timestamp/hash/decision. |
| 64 | `EVIDENCE_NEEDED` | DB-gap matrix is now about 808,210 seconds old and exceeds the 24-hour gate. Regenerate the repository-local structural scan; live DB/Auth/RLS conclusions still require separately proven project-scoped read-only authority. |
| 65 | `EVIDENCE_NEEDED` | Fixed boot proof is about 3,488,858 seconds old and records boot started but not boot success. A fresh port-isolated boot run is required after wrapper ownership and port/process gates are proven. |
| 66 | `EVIDENCE_NEEDED` | Fresh in-app Browser proof now covers the task-owned rank-22-26 runtime: HTTP `200`, source/served asset hashes matched, the `AbandonWare AI` workspace rendered, and warning/error console entries were zero. The settled UI correctly reported external model proof unavailable, and no prompt was sent, so provider/wire success remains `not_observed`. Computer Use stopped before input because it could not determine the existing Chrome window URL with enough policy confidence; fresh Windows UI proof is still needed. |
| 67 | `EVIDENCE_NEEDED` | Fresh read-only `goal_next_auto.ps1 -Status` now exists (SHA-256 `8BC8D6D7...109DC3BE`) and safely reports `statusDecision=ok`, `staleLatest=true`, `failureClassification=stale-latest`, and first action `audit-broad-catches-redacted-breadcrumbs`; the automatic latest packet itself still needs its bounded refresh. |
| 68 | `EVIDENCE_NEEDED` | Fresh scoped completion audit (SHA-256 `152C9E65...2CCA16B`) now reports `local_control_tower_incomplete`, five hard failures, ten total failures, and zero raw-secret-pattern hits. This corrects the stale six-failure count but still requires DB-gap, scorecard, command/collection packet, and SMB debug evidence. |
| 69 | `EVIDENCE_NEEDED` | The 6,378-test host-isolated green is newer than old default-output class-loading failures, but neither is current for today’s dirty tree. Use `verify_full_test_refresh.ps1` only in an owner-coordinated isolated window and retain fresh totals. |
| 70 | `EVIDENCE_NEEDED` | Three controlled provider scenarios are explicitly skipped unless their opt-in mode is selected. Green local tests do not prove a provider/wire attempt; retain `not_observed` until one authorized redacted canary supplies attempt evidence. |
| 71 | `EVIDENCE_NEEDED` | Current porcelain has 1,689 entries (636 modified, 171 deleted, 882 untracked). Obtain an owner manifest for intended files/hunks or a clean comparison worktree; do not stage, reset, or clean. |
| 72 | `EVIDENCE_NEEDED` | Untracked count is now 882, including large main/src/scripts/docs groups. A read-only owner/purpose manifest is required before any isolation or cleanup decision. |
| 73 | `EVIDENCE_NEEDED` | Exactly 171 tracked deletions remain with ambiguous provenance. Require per-file deletion ownership and active-source reachability before accepting or restoring any deletion. |
| 74 | `EVIDENCE_NEEDED` | There are 36 worktrees, four prunable and six detached. Prunable metadata does not prove disposability; require owner/branch/last-use acknowledgement before any prune/delete operation. |
| 75 | `EVIDENCE_NEEDED` | Prior count four is stale: zero active top-level patches exist, while eight nested nonterminal patch bodies remain under three producer roots. Run the read-only janitor inventory and obtain per-producer promotion/retirement decisions. |
| 76 | `EVIDENCE_NEEDED` | Broad bounded roots contain 189 PID files: 184 numeric PIDs are dead, four are live (three evidently reused by unexpected process types), and one is nonnumeric. Never terminate or delete from PID-file evidence alone; require command-line/port/owner attestation. |
| 77 | `EVIDENCE_NEEDED` | Current diff-stat emits 622 LF-to-CRLF warnings, up from 615. Any normalization needs a separately approved minimal path manifest and semantic-diff proof; no bulk rewrite is authorized. |
| 78 | `HOLD` | `holdScope=sleep-test-timeout-contracts`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock`; `independentWorkCompleted=43 Thread.sleep calls and seven actual @Timeout annotations recounted; focused hung-fixture/bounded-normal-case contract identified`; `repositoryWideHold=false`. |
| 79 | `HOLD` | `holdScope=test-system-property-restoration`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock`; `independentWorkCompleted=30 setProperty calls across 12 files versus 15 clears; exact prior-value restoration pattern and preseeded-property RED identified`; `repositoryWideHold=false`. |
| 80 | `FIXED` | Added the owned active-sourceSet `ChatWorkflowLiteralRoutingRegressionTest`: generic `TOKEN/CODE/VALUE/WORD` cases cover both public classification and fallback, with `READY`, explicitly named `TOKEN`, and `GPU3060-PROBE` positive controls. The same test artifact proves rank 14 and rank 80 once at their shared production seam. |
| 81 | `EVIDENCE_NEEDED` | Fresh active-root count is 3,263 broad catches, not 3,278. The prior estimated silent subset is not a durable location list; select one reproducible active catch/caller and prove externally incorrect behavior without a redacted breadcrumb before patching. |
| 82 | `NO_PATCH_NEEDED` | `ChatWorkflow` is 13,086 lines with 222 catches, but size/concentration is not a new causal defect. Preserve the canonical owner and repair only focused behavior seams such as ranks 14/80. |
| 83 | `NO_PATCH_NEEDED` | `WebFailSoftSearchAspect` remains 5,888 lines with 161 catches. Its fail-soft role contains classified provider/timeout handling; no independent wrong fallback or missing taxonomy regression was proven. |
| 84 | `NO_PATCH_NEEDED` | `NaverSearchService` remains 4,705 lines with 75 broad catches. Sampled catches retain bounded redacted evidence, and prior provider audit found no new active defect requiring a size-driven refactor. |
| 85 | `NO_PATCH_NEEDED` | `ChatApiController` remains 4,300 lines with 111 catches. Review concentration is real, but no new HTTP/SSE contract failure follows from LOC; focused SSE work is already covered by rank 11. |
| 86 | `NO_PATCH_NEEDED` | `HybridWebSearchProvider` is now 4,167 lines/120 broad catches, but repeated handling is not itself interrupt loss. The proven interrupt defect was fixed and verified under rank 8. |
| 87 | `NO_PATCH_NEEDED` | `UnifiedRagOrchestrator` remains 3,878 lines. Six-subsystem span is verification pressure, not evidence of a broken decision; retain cross-subsystem tests and patch only a reproduced contract. |
| 88 | `NO_PATCH_NEEDED` | Canonical `service/rag/HybridRetriever` is 2,597 lines; the separate 355-line alias does not change ownership. LOC alone proves no retrieval identity/result defect. |
| 89 | `NO_PATCH_NEEDED` | Canonical `service/guard/EvidenceAwareGuard` is 2,497 lines; the smaller same-simple-name guard is a distinct seam. No guard bypass was reproduced, so merging/extraction would add risk without a causal fix. |
| 90 | `HOLD` | `holdScope=model-runtime-snapshot-retention`; `firstBlockingRule=index-lock-conflict`; `blockingEvidence=index lock plus dirty ModelRuntimeHealthTracker`; `independentWorkCompleted=rank-28 unbounded provider/model snapshots map reconfirmed against bounded sibling stores; capacity+1 deterministic-eviction RED ready`; `repositoryWideHold=false`. |
| 91 | `NO_PATCH_NEEDED` | `DynamicContextCompressor` remains 2,248 lines. Size/four-subsystem span alone proves no compression regression; preserve the `PromptBuilder.build(PromptContext)` boundary. |
| 92 | `NO_PATCH_NEEDED` | Active Self-Ask retriever is now 2,271 lines, not 2,221. Structural pressure is not a new defect; the concrete worker-termination/capacity issue was fixed under rank 9. |
| 93 | `FIXED` | This structural rank links to the same one-time active `QueryTransformer` patch and deterministic termination proof as rank 19. No broad large-file refactor was performed: the existing single-flight map, breaker/cooldown logic, dirty neighboring hunks, and executor lifecycle were retained while only the owned cancellation handle and interrupt-preservation seam changed. |
| 94 | `NO_PATCH_NEEDED` | `RagFailureBlackboxService` remains 2,116 lines/13 catches. Four-subsystem span alone is not a behavior failure and does not justify extraction. |
| 95 | `NO_PATCH_NEEDED` | `DynamicRetrievalHandlerChain` is 2,083 lines/57 catches. The seven-subsystem label is a structural heuristic; no entry-path or retrieval-result failure was established, so the untracked owner should not be edited merely for LOC. |
| 96 | `NO_PATCH_NEEDED` | `HybridWebSearchEmptyFallbackAspect` is 2,067 lines/65 catches. No observed empty-search route proved an incorrect fallback or provider-success claim; do not refactor the dirty aspect for size alone. |
| 97 | `NO_PATCH_NEEDED` | `ChatUiCoreHeartbeatProbe` is 1,991 lines/14 catches. A probe’s size/span does not prove rendered UI or heartbeat behavior wrong; the untracked owner should remain untouched absent a focused contract. |
| 98 | `NO_PATCH_NEEDED` | `ChatStreamSignalBuilder` is 1,638 lines/two catches. No stream/cancel/payload failure was reproduced, so structural extraction is unjustified. |
| 99 | `NO_PATCH_NEEDED` | Production ownership is clear: controller -> `ChatService` -> canonical `ChatWorkflow`; legacy and patch orchestrators are explicit opt-in/default-disabled, and legacy service variants are not Spring beans. Parallel files are maintenance noise, not an active routing ambiguity. |
| 100 | `NO_PATCH_NEEDED` | Fresh active-root total is 2,092 Java files, not 2,091; 212 cross-subsystem, 47 >=1,000-line cross-subsystem, and 41 runtime >=1,000-line totals remain. These are prioritization metrics, not a whole-tree refactor mandate. |

## Proof artifacts

- Cohort A: `.superpowers/sdd/2026-08-24-priority-100-cohort-a-durable-state/progress.md`
- Cohort B1: `.superpowers/sdd/2026-08-25-priority-100-cohort-b1-security-intake-egress/progress.md`
- Cohort B2: `.superpowers/sdd/2026-08-25-priority-100-cohort-b2-retention-profile-safety/progress.md`
- Cohorts C1-C7: `.superpowers/sdd/2026-08-25-priority-100-cohort-c1-vector-queue-capacity/` through `.superpowers/sdd/2026-08-25-priority-100-cohort-c7-chat-run-nonterminal-expiry/`
- Rank 15/16 shared write seam: `docs/superpowers/specs/2026-08-25-priority-100-rank15-16-execution-journal.md`.
- Rank 17 loader election: `docs/superpowers/specs/2026-08-25-priority-100-rank17-execution-journal.md`.
- Shared ranks 19/93 interruption and worker termination: `docs/superpowers/specs/2026-08-25-priority-100-rank19-93-execution-journal.md`.
- Rank 20 degraded-storage I/O-lock repair: `docs/superpowers/specs/2026-08-25-priority-100-rank20-execution-journal.md`.
- Rank 21 bounded NDJSON writer: `docs/superpowers/specs/2026-08-25-priority-100-rank21-execution-journal.md`.
- Ranks 22-26 stream ownership, bounded job lifecycle, and ETA-window concurrency: `docs/superpowers/specs/2026-08-25-priority-100-rank22-26-execution-journal.md`.
- Fresh read-only goal status: `var/codex-smoke/goal-next-auto.status.json` (SHA-256 `8BC8D6D712DE2D489216F0B5030FED6C0BF8D4910871A7D070EA6AFF109DC3BE`).
- Fresh scoped completion audit: `var/codex-smoke/priority-100-completion-audit-20260825.json` (SHA-256 `152C9E653C6A8AF96DEEE5A00C148D0C5E141CC09E52641870121DCC92CCA16B`; five hard failures; zero raw-secret-pattern hits).

## Current next action

Ranks `22` through `26` are closed. The next source burn-down candidate is rank `27`, but it still requires a fresh active-owner/preimage decision because its current owner was untracked at intake; do not infer that authority from the completed ranks. Independently, refresh `EVIDENCE_NEEDED` lanes only through their named non-mutating proof commands, and do not convert missing Browser, Computer, provider, database, owner, or retention evidence into source changes.
