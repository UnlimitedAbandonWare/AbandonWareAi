# Conversate integration — approved execution contract

Status: ACTIVE. Explicit user approval received 2026-09-12, including scoped implementation, tests, minimum project/sidecar dependencies with version/license/reason, and isolated development execution. No repeated design approval is required. Existing goal tool is ACTIVE but exposes no objective-edit operation; this document carries the expanded objective for subsequent runs. Historical external-only blocker counts do not block new independent work.

Latest execution checkpoint: A6 completed its local preparation/control UI seam. One application source file changed; deferred UI/PCM15 pass, related HTTP3 pass, final resources/bootJar exit0, authenticated 5s delayed-material browser proof and Stop input0 chars. Old selection/control/reconnect responses cannot replace a new assist; stop remains available while pause or material selection awaits. Full objective remains ACTIVE. Next A7 is exact chat-target readiness and bounded real local model quality/latency evidence; existing actual model timeout, semantic retrieval, shared infrastructure and physical-device gaps remain open. See `docs/superpowers/reports/2026-09-12-conversate-preparation-controls.md` and task `a6-verification.json`.

## Objective and order

Own app: explicit start → Korean ASR → final/revised utterance → question selection → authorized prepared material/existing RAG → verified short Korean hint → automatic Display refresh. No camera, Meta AI modification, undocumented overlay/background/raw microphone API, or repeated per-answer activation.

1. A1: authenticated volatile output, test answer 1 then 2, actual local browser proof without LLM.
2. A2: text fixtures through selection, authorized prepared material retrieval and Korean SHOW/ASK/HOLD card. One in-flight generation, bounded waiting work, correction/revision fencing, backchannel suppression.
3. A3: phone microphone and Korean streaming ASR with final/revision/overlap/dedup/cancel. Reuse an active seam; otherwise isolated sidecar. No global model/CUDA/PATH changes.
4. A4: same output on real glasses when account/device/approved HTTPS exist; microphone routing, lock/call/reconnect and 20-minute real-device use. Browser/simulator never equals DEVICE_VERIFIED.
5. B: finish durable job, idempotency and owner-routed cancellation requirements below alongside A. Shared infrastructure acceptance cannot block isolated A implementation.

## Protected boundaries

- Live assist audio, transcript, context and card bodies stay in bounded process memory under new assistId/epoch; no chat history, ctx.memory, vector writes, JDBC/Redis content, browser storage/cache, raw logs or automatic summary retention. Stop/expiry discard buffers and invalidate epoch. Restart loses live session by design.
- Existing permitted async jobs retain input/state/result/owner/lease in RDB. Commit before acceptance; atomic claim and fenced result write; external call outside transaction. Completion plus 24-hour retention differs from execution lease. Unknown external outcomes never automatically rerun.
- Existing sessionId/ctx.memory are read-only. Prepared documents are selected read-only with existing ACL. No shared retriever memory mutation, embedding truncation or permission downgrade.
- Preserve Java 17, LangChain4j 1.0.1, existing properties, opnessl/openssl/TLS/secret loader and other writers' edits. No commit/push/stash/reset/clean/branch change, production migration execution, public deploy, new billing, account/ACL/firewall/TLS/global environment change or unrelated HOLD release.
- Source edits use the existing single three-query preflight and owner lease/preimage checks. Parent owns all writes; bounded native explorer is read-only. Existing GLM transport HOLD remains.

## Lifecycle and admission

General interactive initial/attach streams count only external subscribers and cancel the exact execution when its last subscriber leaves. Completion, attach and cancellation races must be fenced. Cross-instance attach/cancel must route to the execution owner or use verified shared leases; local counts are not global evidence. 202 jobs survive acceptance/status disconnect and use explicit cancel/deadline/lease.

Conversate differs: hide changes presentation only; pause stops capture/new inference; stop destroys queued work and invalidates epoch. Output loss has up to 5-second bounded grace then PAUSED; diagnostic connections are not output leases. Reconnect cannot revive STOPPED. Multiple output subscribers and completion races must be tested.

Generation idempotency uses authenticated actor, operation, key and normalized semantic fingerprint. Running duplicate returns 409/reference; completed duplicate returns authorized stored result/reference; different input is conflict. Durable DB uniqueness survives Redis TTL through retention. Redis failure refuses new expensive work while read/cancel remain available. Live dedup is volatile, keyed by assist/epoch/question/utterance/revision. Preserve local 64/2 concurrency and add trusted user/IP limits; control/status/reconnect are separate cost classes.

## Cards and evidence

Korean Answer first, 2–3 lines, initial maximum 120 characters. Preserve numbers, units and negation; distinguish facts/suggestions. Returned source IDs must be a subset of retrieved permitted evidence. Insufficient support yields ASK/HOLD. Distinguish NO_MATCH, missing key, index error, embedding mismatch, denied, rate limit, timeout and parse failure; only normal NO_MATCH permits bounded query refinement. Conditional positive/negative/neutral reasoning for complex cases, no three-model call per utterance, no exposed internal reasoning.

Report source diff, command/exit, pass/fail/skip and measured sample counts. Separate server cancel, socket closure (existing 2-second test), provider stop (1-second target), browser and physical device proof. Latency targets p95 2.5 seconds local/6 seconds extended are targets, not guarantees.

## Acceptance ledger and next action

The earlier named `codex_meta_display_conversate_20260912.md` was not found in accessible initial repo/doc/data searches; `docs/agent` returned access denied. Its T01–T22 details have not been read or invented. Implement the user's restated requirements independently; map original IDs if the document becomes available.

- A1: locally verified — authenticated automatic fixture cards, bounded volatile lifecycle and measured browser geometry. No device claim.
- A2: locally verified — text selection, existing attachment ACL adapter, local BM25/lexical rerank, conservative Korean SHOW/ASK cards, bounded queue and stale result fencing. Fresh 80 Java tests plus one UI race regression pass; `a2-verification.json` and `a2-browser.json` hold exact evidence. Full vector/semantic/complex-reasoning coverage remains pending.
- A3: local streaming implementation verified — phone AudioWorklet PCM, authenticated epoch-bound bridge and owned CPU child, VAD partial/final/revision/overlap/dedup. Fresh Java98/Python5/Node3 pass, no skipped process test. Three synthetic Korean stream runs produce one final/one answer each; actual ASR-derived card and source visible in browser at600x600. `a3-verification.json` records samples and hashes. Physical phone capture/lock/call/Bluetooth/HTTPS remain unverified. No global environment/model change.
- A4: evidence_needed — real device/account and authorized HTTPS not established.
- B1: fresh31 tests pass for durable task idempotency, completed reference, conflict/owners/retention and restart. Two service instances use one isolated H2 file; simultaneous5 and staggered5 at100ms each invoke the fixture handler once. No production migration or actual provider exactly-once claim. `b1-verification.json` freezes evidence; later B2/B3 status follows below. Previous211 tests are historical only.
- B2: implemented and verified with115 Java tests including live correction fixes, plus version/source-set/app/bootJar checks. Existing strict Redis Lua reused at actual task/assist/ASR admission; captured actor/IP hashes only, no request retained. Redis failures refuse new costly work; task replay/read/cancel bypass. Canonical JSON hashing preserves meaningful whitespace/array order/null flags; operation and effective execution headers included. Conflict409. Isolated fixture omits distributed admission; mocks verify response handling, not real shared Redis.
- B3: implemented exact bounded JSON/SSE final result storage under existing owner/operation claim. Successful normal-chat response advice and post-persistence SSE final callback mark semantic completion; HTTP end alone leaves OUTCOME_UNKNOWN. Same key replays original result after later turns and Redis loss; completed results cascade with24h parent retention. Legacy completed records without result remain409; oversized>2MiB results retain an uncertain fence. Client keeps one in-memory WeakMap key per logical stream payload, excludes attach and skips obsolete final ACK on replay. Fresh143 Java tests plus55 affected chat-boundary tests pass. UI key contract, stream/fault/20 restore contracts pass. New migration only tested on isolated H2.
- Live refinements: epoch changes clear dedup ledger; deterministic amount/unit/negation corrections across new utterance IDs cancel through the existing worker fence, preserving original text. Fresh browser found stale remote-stop caption/reconnect; fixed with a terminal UI latch and five Node tests, including PCM/key contracts. This classifier does not claim general Korean paraphrase understanding.
- External DB/Redis and actual provider stop: acceptance evidence_needed; historical reports remain supporting evidence only.

Next execution reads this file and the latest checkpoint under `data/agent-handoff/conversate/20260912-01a09365/`, then continues the first runnable item. Never repeat an unchanged external-blocker audit while A work is available.

## Next implementation: B4 and remaining A coverage

No source-backed cross-instance execution directory, instance identity or trusted internal relay exists in ChatRunRegistry/ChatCancellationCommandHandler. Add an explicitly configured owner directory and authenticated owner relay at the existing exact-run boundary; keep one owner-side external subscription per relayed viewer. A lost relay/process must release that viewer; one remaining viewer must keep its execution alive. Prove two loopback instances with synthetic credentials before claiming local routing. Actual deployment/LB/shared DB/Redis configuration remains separate evidence. Do not use local counts as global counts, publish arbitrary directory URLs, or forward user credentials to untrusted hosts. Do not apply this immediate general-chat policy to Conversate's5s grace or202 jobs.

The prepared attachment ACL seam is reusable. A bounded exploration found attachment-local optional bi-encoder/ONNX/DPP reranking, but no proven ACL-filtered global vector seam. Inspect the chosen reranker's privacy/availability before reuse; do not import the shared ChainContext/TraceStore or weaken ACL. Conditional complex-role reasoning, comprehensive search failure taxonomy, phone microphone/lock/call/Bluetooth, actual Display and20-minute real use remain incomplete.

## A3 implementation decision

Use an explicitly configured, task-owned Python child over bounded stdin/stdout JSON lines. No new network listener, hosted ASR bill or shared model process. Browser captures phone microphone only after explicit activation and sends 16 kHz mono PCM through the existing authenticated/CSRF-protected assist boundary. Audio frames, VAD segments, partial/final revisions and overlap dedup remain bounded memory. One child globally initially, one inference at a time, bounded pending partial/final work; controls bypass the input queue. Pause/stop/expiry close only the owned child and clear browser tracks/queues. Actual child exit is separately measured; it is no proof of remote GPU cancellation. Feature/config absence leaves microphone unavailable without affecting existing chat or text assist. A locked/background phone and Bluetooth routing remain real-device acceptance items.

## B4 local implementation update

The previous B4 next-action paragraph is superseded by the opt-in owner directory/relay implementation in `ChatRunRegistry`, `ChatRunOwnerDirectory`, `ChatRunCluster`, `ChatRunClusterController`, and additive migration05. No new dependency, shared DB migration execution, deployment credential, TLS/firewall setting, branch or production process was changed. Directory claim precedes every registry-owned producer; process boot/lease fences prevent stale renewal and automatic takeover. Public ACL remains before exact attach/cancel/ACK/state forwarding. Peer requests are HMAC authenticated to explicitly allowlisted HTTPS origins (literal loopback HTTP for isolated tests), with replay-nonce bounds and separate stream/control connection pools. Shared current-token selection and deletion fencing cover a new owner on another node. Ordinary chat remains separate from live-assist grace and202 lifetime.

Fresh `b4-regression.log` exit0:169 tests, failure/error/skip0, including10 loopback cluster tests and5 directory tests. Final sampled relay disconnect-to-owner-cancel-handler357ms,n=1, separately checked terminal cleanup. This is not physical packet capture, provider stop, separate-JVM kill, shared production topology or device verification. Full Conversate goal remains ACTIVE and incomplete. See `docs/superpowers/reports/2026-09-12-chat-owner-routing.md` and the stage checkpoint for exact limitations and build proof.

## Next executable A work and preserved external gaps

An additional read-only trace found `OnnxRuntimeService.available()`/`active()` always false in current source. Its `scorePair` is a lexical Jaccard fallback, not an available semantic ONNX model; the wrapper stable-trims when unavailable. Do not count this as semantic reranker integration or add a duplicate lexical chain. Hash/count/reason telemetry is allowed under the approved privacy contract; raw live content sinks or unverified outward embedding calls are not. Existing prepared attachment ACL and local BM25/lexical path remain reusable.

Continue by tracing the existing stateless local generation/embedding boundary for content retention and availability, then implementing the smallest volatile-only optional semantic/complex-card path with explicit unavailable reasons, budgets, real source-ID validation, number/unit/negation counterexamples and no new live-history or memory writes. Missing local models must not be masked as successful semantic results. Complete the search failure taxonomy and permit at most one bounded rewrite only for normal NO_MATCH. Preserve the already functioning text/ASR-to-extractive-card path during this work. Real phone capture/lock/call/Bluetooth, actual Display/Neural Band, allowed HTTPS, shared MariaDB/Redis deployment and provider-compute-stop evidence remain separate acceptance lanes, not a repository-wide HOLD.

## A4 checkpoint and next executable action

A4 local generation code is implemented at the existing volatile assist seam. Seven source paths changed, no dependency added. Default OFF, explicit literal-loopback native model config, one bounded call without CPU/provider fallback, isolated prompt boundary, actual retrieved evidence IDs and conservative number/unit/negation/condition/wording verification. Existing worker interruption, actual in-flight capacity and epoch fencing remain the cancellation owner. One structured model assessment is not three independent models. Short/no-match/conflicting numeric questions keep the existing path.

Fresh90 Java tests and4 UI/PCM tests pass; version/source-set/app/bootJar checks pass. Authenticated real browser showed the fixture-generated Korean card/source at600x600 without overflow; five controls88px. Plain assist interruption-to-peer EOF/reset1ms,n1 is separate from GPU stop. Actual installed gemma3:4b local probe timed out4201ms on its first4s-budget request; stopped after1 request. Actual model quality/latency acceptance FAIL, not a passing completion just because probe exit0. See `docs/superpowers/reports/2026-09-12-conversate-local-generation.md` and `a4-verification.json`.

Continue A5 NO_MATCH-only bounded rewrite and search taxonomy as independent work. Narrow the actual local-model bottleneck with fresh runtime evidence before another explicitly bounded attempt; do not raise the deadline or weaken source checks to claim success. General semantic/vector/graph/web integration and real phone/device/shared-infrastructure acceptance remain incomplete. Full goal ACTIVE, approval persists, no new approval question.

## A5 implemented; next A6 work

A5 is locally implemented: only genuine BM25 NO_MATCH permits one unique two-word spacing repair using the selected authorized vocabulary; no generic facets, truncation, model or external query. Existing normalization incorrectly removed final 가 from 수리불가; a fresh failing test drove the narrow negation-preserving repair. Actual search/refinement counters are visible. Prepared DB and transaction-boundary failures are no-store503; empty parsed content is422; existing ACL remains. Fresh43 assist tests and4 UI/PCM tests pass, final version/source-set/app/bootJar exit0. Authenticated browser observed compound→grounded answer, changed quantity→ASK, backchannel→no extra search, then STOPPED. See `docs/superpowers/reports/2026-09-12-conversate-search-refinement.md` and `a5-verification.json`.

A6 first reproduces the observed UI preparation race: prevent new text/audio submission against the old epoch while a material selection is pending, retain typed input, keep stop/pause usable, then prove the prepared card path in browser. A second bounded read-only trace found existing LocalLlmProcessManager opt-in warmup/diagnostics; chat-target readiness needs exact endpoint/model/freshness, not default embedding warmup or listener health. Before any new actual model request, freeze readiness/request/retry ceilings and preserve shared processes. Do not increase live card deadlines or claim general semantic validation. Physical phone/glasses, real model quality, global semantic index and shared infrastructure remain incomplete; full goal ACTIVE.
