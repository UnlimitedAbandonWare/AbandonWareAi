# soft-evidence flat props (avoid YAML duplicate keys)
guard.citation.min_count=0
gate.finalSigmoid.threshold=0.50
gate.finalSigmoid.mode=log-only
gate.citation.min=0
jammini.guard.evidence.min-coverage=0.05
jammini.guard.mode=soft
gate.evidence.allow-empty=true
guard.citation.require_official=false
verifier.coverage.threshold.default=0.02
gate.citation.log-only=true
jammini.memory.min-coverage=0.05
