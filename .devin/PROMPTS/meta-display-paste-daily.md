# Meta Ray-Ban Display — 일상 패치용 (복붙)

아래만 입력창에 붙여 넣고, 그다음 줄에 목적만 적어.

@objective-executor @demo1-devin-source-orchestrator @meta-rayban-display @demo1-meta-display-simple-caption @demo1-core-request-router @demo1-agent-api-spend-guard

판단이 애매할 때만 추가: @positive-negative-neutral-judge
이음새 2개 이상(힌트 과거맥락 + 백그라운드 수음 등)이면 목적 본문을 `--brief-file`로 `python -B scripts/devin_task_orchestrate.py plan`에 넣고 단계만 따른다.

Project Root는 이 워크스페이스. Fold6=mic, 렌즈=조용한 텍스트 힌트(4~8줄). 최소 diff. 비밀값 금지. openssl 키 구조 변경 금지. 큰 패치=작업 직전 사본+타깃 범위 잠금. 「모드」 말 쓰지 마.

목적:
