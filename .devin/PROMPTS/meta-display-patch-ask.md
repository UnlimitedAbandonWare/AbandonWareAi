# Devin Meta Display 패치 요청

**추천(최소 복붙):**
@objective-executor @demo1-devin-source-orchestrator @meta-rayban-display

애매하면 추가: @positive-negative-neutral-judge

더 많은 칩이 필요하면 `meta-display-paste-daily.md` / `meta-display-paste-debug.md` 참고. 스킬 30개 이상 붙이지 말 것.
이음새 2개 이상이면 `python -B scripts/devin_task_orchestrate.py plan --brief-file <path>` 단계를 따른다.

Project Root: 이 워크스페이스. Fold6=mic, 렌즈=조용한 텍스트(4~8줄). 최소 diff. 비밀·openssl 키 구조 금지. 큰 패치=작업 직전 사본+타깃 범위 잠금. 「모드」 금지.

목적:
(여기)
