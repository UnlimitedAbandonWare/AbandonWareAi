package config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebInfraConfig {
    // Upstash/Caffeine wiring can be added here when credentials provided.
    // Keeping it minimal to avoid runtime failures without secrets.
}