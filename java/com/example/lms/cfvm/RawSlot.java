package com.example.lms.cfvm;

/** Placeholder RawSlot with nested Stage enum to satisfy ToyMatcher signature. */
public final class RawSlot {
    private RawSlot(){}
    public enum Stage { START, MIDDLE, END }
}