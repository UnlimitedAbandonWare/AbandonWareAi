package com.abandonware.ai.agent.integrations.math;


/**
 * Low-rank ZCA whitening (interface stub).
 */
public class ZcaWhitening {
    public double[] apply(double[] vec){ return vec == null ? new double[0] : vec.clone(); }
}