# merge130 profile: gpu-3060x2
llmrouter:
  profiles:
    3060x2:
      models:
        light:  { base-url: "http://localhost:8002", weight: 0.55, tag: "3060#1" }
        mid:    { base-url: "http://localhost:8001", weight: 0.25, tag: "3060#0" }
        heavy:  { base-url: "http://localhost:8000", weight: 0.20, tag: "3060#0" }
zsys:
  onnx:
    device-id: 1

train_idle:
  enabled: true

autolearn:
  enabled: true

llm:
  provider: local
  base-url: http://localhost:11434/v1
  chat-model: gemma3:27b

  api-key: dummy
fallback:
  enabled: false

jammini:
  guard:
    profile: PROFILE_MEMORY
  memory:
    progressive: true

naver:
  filters:
    domain-policy: boost
    enable-domain-filter: false
