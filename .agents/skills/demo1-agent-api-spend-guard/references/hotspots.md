# Hotspots that burned credits in agent sessions

- `scripts/chat_ui_vibe_soak.ps1` — iterative live soak
- `scripts/device_probe_gpt_*.ps1` — multi-role GPT probe/compare
- Smoke scripts under `scripts/smoke_*` that export `OPENAI_API_KEY` / search keys
- `llm.gateway.cloud.enabled=true` default + `api3` route
- FallbackBanner / ensemble / Self-Ask escalation to cloud
- Stale `gpt-4` defaults in `configs/models.manifest.yaml` and some profile YAMLs
