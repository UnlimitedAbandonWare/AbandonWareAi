display_name: Build Error Miner
short_description: Classify build logs without raw log dumps.
default_prompt: Use build_error_miner through scripts\awx_mcp_toolbox.ps1 to classify build or boot logs into stable failure classes with redacted output.
