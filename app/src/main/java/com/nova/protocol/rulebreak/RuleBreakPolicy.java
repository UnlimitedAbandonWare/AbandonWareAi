package com.nova.protocol.rulebreak;
/**
 * [GPT-PRO-AGENT v2] - concise navigation header (no runtime effect).
 * Module: com.nova.protocol.rulebreak.RuleBreakPolicy
 * Role: config
 * Observability: propagates trace headers if present.
 * Thread-Safety: appears stateless.
 */
/* agent-hint:
id: com.nova.protocol.rulebreak.RuleBreakPolicy
role: config
*/
public enum RuleBreakPolicy {
    fast, broad, deep
}