package com.example.lms.strategy;

/**
 * Compatibility source retired in favor of the canonical root-module owner.
 *
 * <p>This compilation unit intentionally declares no Java type, so the app
 * source set cannot redefine the runtime FQCN.
 */
