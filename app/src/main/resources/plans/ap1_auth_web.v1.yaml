# MERGE_HOOK:PROJ_AGENT::FIM15
llm:
  provider: local
  model: ${llm.high.model:${llm.chat-model}}

name: AP1_AUTH_WEB
version: '1'
params:
  officialSourcesOnly: true
  web_top_k: 12
chain:
- alias_correct
- retrieve_web_official
- fuse_rrf
- rerank_cross
- gate_citation
- final_sigmoid

# NineTile alias corrector toggle
alias:
  corrector:
    enabled: true

probe:
  search:
    enabled: false
  admin-token: ''


retrieval:
  vector:
    enabled: true