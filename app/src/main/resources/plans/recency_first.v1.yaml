# MERGE_HOOK:PROJ_AGENT::FIM15
llm:
  provider: local
  model: ${llm.high.model:${llm.chat-model}}

id: recency_first.v1
k: { web: 12, vec: 5, kg: 2 }
enable: { ocr: false, dpp: false }
thresholds: { minScore: 0.15 }
gates: { citationsMin: 3 }

probe:
  search:
    enabled: false
  admin-token: ''


retrieval:
  vector:
    enabled: true