# MERGE_HOOK:PROJ_AGENT::FIM15
llm:
  provider: local
  model: ${llm.high.model:${llm.chat-model}}

id: safe_autorun.v1
k-allocation:
  web: 8
  vector: 8
  kg: 0
gates:
  citation-min: 2
  official-only: true
rerank:
  bi-enabled: false
  onnx-enabled: false

guard:
  profile: PROFILE_MEMORY
