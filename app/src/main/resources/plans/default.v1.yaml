# MERGE_HOOK:PROJ_AGENT::FIM15
llm:
  provider: local
  model: ${llm.high.model:${llm.chat-model}}

id: default.v1
description: "안정형: 일반 질의용"
retrieval:
  webTopK: 10
  vectorTopK: 8
  kgTopK: 0
gates:
  whitelistRequired: true
  minCitations: 2
  dppEnabled: true
  calibrator: minmax
  mpLawEnabled: true
constraints:
  onnxMaxConcurrency: 4
  timeBudgetMs: 6000

probe:
  search:
    enabled: false
  admin-token: ''

  vector:
    enabled: true