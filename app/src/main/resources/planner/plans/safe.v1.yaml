# safe.v1.yaml — conservative plan
version: 1
name: safe.v1
k-allocation: { web: 8, vector: 4, kg: 2 }
gates:
  autorun: { min-citations: 2, min-evidence: 2 }
  citation: { min: 2 }
flow:
  - analyze
  - retrieve:web
  - retrieve:vector
  - fuse:rrf
  - rerank:bi
  - rerank:cross
  - build:prompt

# NineTile alias corrector toggle
alias:
  corrector:
    enabled: true

probe:
  search:
    enabled: false
  admin-token: ''


retrieval:
  vector:
    enabled: true