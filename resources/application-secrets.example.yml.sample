spring:
  config:
    activate:
      on-profile: secrets

search:
  brave:
    enabled: true
    api-key: "REPLACE_ME"

upstash:
  vector:
    rest-url: "https://<REPLACE_ME>"
    api-key: "REPLACE_ME"
    namespace: "aw-default"
    read-only: true

llm:
  groq:
    key: "REPLACE_ME"