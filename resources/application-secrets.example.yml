spring:
  config:
    activate:
      on-profile: secrets

search:
  brave:
    enabled: true
    api-key: "${API_KEY}"

upstash:
  vector:
    rest-url: "https://<${GENERIC_LONG_TOKEN}>"
    api-key: "${API_KEY}"
    namespace: "aw-default"
    read-only: true

llm:
  groq:
    key: "${KEY}"