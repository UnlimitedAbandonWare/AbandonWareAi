name: demo1_design_selfask_corrector
version: "1.0.0"
language: ko
scope: design-validation-before-patch
parent: demo1_p0_safe_patch_orchestrator
description: >
  설계 입력을 받으면 시키는 대로 즉시 구현하지 않는다.
  Self-Ask 재교정 루프로 설계 오류를 먼저 탐지·교정한 뒤,
  교정된 설계를 바탕으로 최소 diff 패치 계획을 확정한다.
tags:
  - design-validation
  - selfask
  - anti-blindly-implement
  - safe-patch
