# 40_RETROSPECT.md — 중간 회고

- 리스크: 80/443은 리눅스에서 특권 포트로 권한 필요(루트 또는 setcap). 배포 환경에서 권한 정책 확인 필요.
- 선택: 설정 파일을 바꾸지 않기 위해 **코드 레벨 포트 재지정**을 채택. 운영에서 `security.force-https=true`로 HTTPS 강제 가능.
- 추후: 리버스 프록시(Nginx) 전면 배치 시 내장 톰캣은 8080(HTTP)만 두고 프록시에서 TLS 종단/리디렉션 수행 권장.
