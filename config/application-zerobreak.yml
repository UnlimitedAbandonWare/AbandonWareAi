spring:
  config:
    activate:
      on-profile: zerobreak
zerobreak:
  enabled: true
  require_signed_token: true
  default_policy: [recency, max_recall]
  whitelist_override: conditional  # off|conditional|on
  citation_gate_min_sources: 3
  final_sigmoid_threshold: 0.90
  banner_text: "【주의: 확장 탐색(Zero Break) 모드 적용】"
  header_names:
    token: X-ZeroBreak-Token
    policy: X-ZeroBreak-Policy
naver:
  search:
    web-top-k: 24
    timeout-ms: 3500
  hedge:
    enabled: true
    delay-ms: 120
retrieval:
  vector:
    enabled: true
  fusion:
    rrf:
      weighted: true
onnx:

budget:
  request-time-ms: 3500

naver:
  hedge:
    enabled: true
    delay-ms: 120
  search:
    timeout-ms: 1200
    web-top-k: 12

ocr:
  enabled: true
  min-confidence: 0.78

probe:
  search:
    enabled: false
  admin-token: ''